里氏替换
如果每个类型A1的对象都有类型A2的对象，使得A1定义的所有程序P在所有A1对象
替换为A2时，程序P行为没有变化，那么A2是A1的子欸行，（所有引用基类的
地方必须能透明的使用其子类的对象）
在子类中尽量不要重写父类的方法
继承使两个类耦合性增强了，在适当情况下，可以通过聚合，组合，依赖来解决问题
继承体系的复用性比较差

通用做法是 原来的父类和子类都继承一个基本类，原有继承关系去掉
采用依赖、聚合、组合